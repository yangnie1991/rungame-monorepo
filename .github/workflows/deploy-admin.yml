name: Deploy Admin to VPS

on:
  push:
    branches:
      - main                        # 仅主分支触发自动部署
    paths:
      - 'apps/admin/**'
      - 'packages/database/**'
      - 'Dockerfile.admin'
      - 'docker-compose.admin.yml'
      - '.github/workflows/deploy-admin.yml'
  workflow_dispatch:  # 允许手动触发
    inputs:
      branch:
        description: '要部署的分支（留空则使用当前分支）'
        required: false
        default: ''
      environment:
        description: '部署环境'
        required: false
        type: choice
        default: 'production'
        options:
          - production
          - staging
          - development

jobs:
  deploy:
    name: Deploy to VPS via SSH
    runs-on: ubuntu-latest

    steps:
      # ==========================================
      # 1. 检出代码
      # ==========================================
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || github.ref }}

      # ==========================================
      # 2. 设置 pnpm 和 Node.js 环境
      # ==========================================
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      # ==========================================
      # 3. 获取分支信息
      # ==========================================
      - name: Get branch info
        id: branch
        run: |
          # 获取分支名称（去掉 refs/heads/ 前缀）
          BRANCH_NAME="${{ github.event.inputs.branch || github.ref_name }}"
          echo "branch_name=${BRANCH_NAME}" >> $GITHUB_OUTPUT
          echo "部署分支: ${BRANCH_NAME}"
          echo "部署环境: ${{ github.event.inputs.environment || 'production' }}"

      # ==========================================
      # 4. SSH 部署到 VPS
      # ==========================================
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        env:
          DEPLOY_BRANCH: ${{ steps.branch.outputs.branch_name }}
          DEPLOY_ENV: ${{ github.event.inputs.environment || 'production' }}
          # ============================================
          # 共享环境变量（根目录 .env）
          # ============================================
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          NODE_ENV: ${{ secrets.NODE_ENV || 'production' }}
          NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
          NEXT_PUBLIC_GA_ID: ${{ secrets.NEXT_PUBLIC_GA_ID }}
          NEXT_PUBLIC_ADSENSE_ID: ${{ secrets.NEXT_PUBLIC_ADSENSE_ID }}
          # ============================================
          # Admin 特定环境变量（apps/admin/.env.local）
          # ============================================
          CACHE_DATABASE_URL: ${{ secrets.CACHE_DATABASE_URL }}
          NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
          NEXTAUTH_URL: ${{ secrets.NEXTAUTH_URL }}
          ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY }}
          # R2 存储配置
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
          R2_PUBLIC_URL: ${{ secrets.R2_PUBLIC_URL }}
          # Google Search API 配置（可选）
          GOOGLE_SEARCH_API_KEY: ${{ secrets.GOOGLE_SEARCH_API_KEY }}
          GOOGLE_SEARCH_ENGINE_ID: ${{ secrets.GOOGLE_SEARCH_ENGINE_ID }}
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          envs: DEPLOY_BRANCH,DEPLOY_ENV,DATABASE_URL,NODE_ENV,NEXT_PUBLIC_SITE_URL,NEXT_PUBLIC_GA_ID,NEXT_PUBLIC_ADSENSE_ID,CACHE_DATABASE_URL,NEXTAUTH_SECRET,NEXTAUTH_URL,ENCRYPTION_KEY,R2_ACCOUNT_ID,R2_ACCESS_KEY_ID,R2_SECRET_ACCESS_KEY,R2_BUCKET_NAME,R2_PUBLIC_URL,GOOGLE_SEARCH_API_KEY,GOOGLE_SEARCH_ENGINE_ID
          script: |
            # 设置错误时退出
            set -e

            echo "=========================================="
            echo "开始部署 RunGame 管理端"
            echo "分支: ${DEPLOY_BRANCH}"
            echo "环境: ${DEPLOY_ENV}"
            echo "时间: $(date)"
            echo "=========================================="

            # 检查并初始化项目目录
            if [ ! -d "/opt/1panel/docker/compose/rungame-admin" ]; then
              echo "📁 项目目录不存在，创建并克隆仓库..."
              mkdir -p /opt/1panel/docker/compose
              cd /opt/1panel/docker/compose
              git clone https://github.com/${GITHUB_REPOSITORY}.git rungame-admin
              cd rungame-admin
            else
              # 进入已存在的项目目录
              cd /opt/1panel/docker/compose/rungame-admin

              # 检查是否为 Git 仓库
              if [ ! -d .git ]; then
                echo "⚠️  目录存在但不是 Git 仓库，重新初始化..."
                cd ..
                mv rungame-admin rungame-admin.backup.$(date +%Y%m%d_%H%M%S)
                git clone https://github.com/${GITHUB_REPOSITORY}.git rungame-admin
                cd rungame-admin
              else
                # 拉取最新代码
                echo "📦 拉取最新代码..."
                git fetch origin
              fi
            fi

            # 切换到指定分支
            echo "🔀 切换到分支: ${DEPLOY_BRANCH}"
            git checkout ${DEPLOY_BRANCH}
            git reset --hard origin/${DEPLOY_BRANCH}

            # ============================================
            # 生成 Admin 应用环境变量配置文件
            # 注意：仅部署 Admin，所有变量合并到 apps/admin/.env.local
            # ============================================
            chmod +x .github/scripts/generate-env.sh
            .github/scripts/generate-env.sh

            # 备份当前容器（如果存在）
            if docker ps -a --format '{{.Names}}' | grep -q "^rungame-admin$"; then
              echo "💾 备份当前容器..."
              docker commit rungame-admin rungame-admin:backup-$(date +%Y%m%d_%H%M%S) || true
            fi

            # 停止并删除旧容器
            echo "🛑 停止旧容器..."
            docker-compose -f docker-compose.admin.yml down || true

            # 清理未使用的镜像
            echo "🧹 清理未使用的镜像..."
            docker image prune -f || true

            # 构建新镜像（利用 Docker 层缓存，加快构建速度）
            echo "🔨 构建新镜像..."
            docker-compose -f docker-compose.admin.yml build

            # 启动新容器
            echo "🚀 启动新容器..."
            docker-compose -f docker-compose.admin.yml up -d

            # 等待服务启动（standalone 模式启动速度提升 50%）
            echo "⏳ 等待服务启动..."
            sleep 8

            # 健康检查（对齐 Dockerfile HEALTHCHECK 的 start-period=40s）
            echo "🔍 执行健康检查..."
            for i in {1..20}; do
              if curl -f http://localhost:4000/api/health > /dev/null 2>&1; then
                echo "✅ 服务已就绪！"
                break
              fi
              echo "等待服务就绪... ($i/20)"
              sleep 2
            done

            # 显示容器状态
            echo "📊 容器状态:"
            docker ps --filter name=rungame-admin

            echo "=========================================="
            echo "✅ 部署完成！"
            echo "时间: $(date)"
            echo "=========================================="

      # ==========================================
      # 5. 部署结果通知（可选）
      # ==========================================
      - name: Send notification on success
        if: success()
        run: |
          echo "✅ 管理端部署成功"
          # 这里可以添加通知逻辑，如发送邮件、Slack、钉钉等

      - name: Send notification on failure
        if: failure()
        run: |
          echo "❌ 管理端部署失败"
          # 这里可以添加失败通知逻辑
