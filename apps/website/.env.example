# ============================================================
# Website 应用特定环境变量
# ============================================================

# Website 端为纯展示应用，不需要认证功能
# 所有必需的环境变量都在根目录的 .env 文件中配置

# ============================================================
# 可选配置
# ============================================================

# 在开发环境启用数据缓存（默认关闭）
# 启用后可以测试缓存行为，但会导致数据更新延迟
# 生产环境始终启用缓存，此选项仅影响开发环境
# ENABLE_DEV_CACHE="true"
