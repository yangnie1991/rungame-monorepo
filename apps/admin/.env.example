# ============================================================
# Admin 应用环境变量示例
# ============================================================

# ========== 数据库配置 ==========
# 业务数据库（与 Website 共享）
DATABASE_URL="postgresql://user:password@host:port/db_name?schema=public"
# Admin 专用管理数据库
CACHE_DATABASE_URL="postgresql://user:password@host:port/db_name_admin?schema=public"

# ========== Better Auth 认证配置 ==========
# 生成密钥: openssl rand -base64 32
BETTER_AUTH_SECRET="your_secret_key_base64"
# 认证基础 URL (通常与 NEXT_PUBLIC_APP_URL 相同)
BETTER_AUTH_URL="http://localhost:4000"
# 客户端应用 URL
NEXT_PUBLIC_APP_URL="http://localhost:4000"
# 信任 Host (反向代理部署时需要)
AUTH_TRUST_HOST=true

# ========== 加密密钥 ==========
# 用于加密数据库中的 API Keys 和敏感配置
# 生成: openssl rand -base64 48
ENCRYPTION_KEY="your_encryption_key_base64_must_be_very_long"

# ========== 运行环境 ==========
NODE_ENV="development"
PORT=4000
HOSTNAME="0.0.0.0"

# ========== 以下配置已迁移到管理后台 ==========
# Cloudflare R2、Google Search、Jina Reader 等外部 API 配置
# 请在管理后台 → 外部 API 配置 中设置
# AI 模型配置请在管理后台 → AI 配置 中设置
# SEO 搜索引擎配置请在管理后台 → SEO 提交 中设置
# ============================================================
