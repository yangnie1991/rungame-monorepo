# ============================================
# RunGame Admin 部署环境变量示例
# 使用方法: cp .env.admin.example .env
# ============================================

# 1. 数据库连接 (必须)
# 格式: postgresql://用户:密码@主机:端口/数据库名?schema=public
# 注意: 如果数据库在宿主机，主机地址通常是宿主机IP，或者Docker网桥IP (172.17.0.1)
DATABASE_URL="postgresql://postgres:password@127.0.0.1:5432/rungame?schema=public"

# 2. NextAuth 认证配置 (必须)
# 生产环境请生成一个强随机字符串: openssl rand -base64 32
NEXTAUTH_SECRET="change-this-to-secure-random-string"

# Admin 访问地址 (必须包含 http:// 或 https://)
NEXTAUTH_URL="http://your-server-ip:4000"

# 3. 数据加密密钥 (必须)
# 用于加密敏感配置，生成方法同上
ENCRYPTION_KEY="change-this-to-secure-random-string"

# 4. 其他配置
NODE_ENV="production"
PORT=4000
